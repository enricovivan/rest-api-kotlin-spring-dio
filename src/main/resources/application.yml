spring:
  datasource:
    url: jdbc:h2:mem:credit_application_system_db
    username: eu
    password:
  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        trace: false
        web-allow-others: false
  jpa:
    show-sql: true
    #hibernate:
    properties:
      hibernate.format_sql: true